import jugador.*
import indicadores.*


describe "Pruebas del personaje"{
	
	fixture{
		timer.iniciar()
		vida.iniciar()
		pasajeros.iniciar()
	}
		
	
	test "verifica que el personaje se mueva un espacio más en el sentido que tenia" {
		personaje.position()
		personaje.mover(1)
		personaje.position()
	}	
}
